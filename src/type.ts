type Vec2 = [number, number];
type Vec3 = [number, number, number];
type Vec4 = [number, number, number, number];

const VEC_3: Vec3 = [...Array(3).keys()] as Vec3;

export { Vec2, Vec3, Vec4, VEC_3 };
